services:
  - type: web
    name: upc-lookup
    env: python
    region: oregon
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
      - key: BARCODE_API_KEY
        sync: false
    healthCheckPath: /
